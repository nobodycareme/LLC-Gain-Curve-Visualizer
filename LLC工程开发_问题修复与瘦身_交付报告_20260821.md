# LLC 增益曲线工具 — 问题修复 / 性能优化 / EXE 瘦身 最终交付报告

- 项目目录：`F:\OneDrive\找工作\电源\LLC\LLC增益曲线_EXE`
- 交付日期：2026-08-21
- 本地 commit：`1f193f6`（未 push GitHub）
- 范围：仅"问题修复 + 性能优化 + EXE 瘦身"，未重新设计数学模型、未增加无关大功能

---

## 0. 修复基线

| 项目 | 值 |
|---|---|
| 启动时 commit SHA | `388fa5c` |
| 交付时 commit SHA | `1f193f6` |
| 基线 EXE 大小 | 27,649,756 bytes（27.65 MB） |
| 交付 EXE 大小 | 21,854,288 bytes（20.84 MB） |
| 交付 EXE SHA256 | `645BDBDC97A8AAD2FC05D957B975BC0B561E733BBA3544C307AEC92C684EECC6` |
| 测试结果 | 1141 passed, 0 failed |

---

## 1. BUG1：取消图层后工作点与当前 Q 曲线错位

**根因**：`_plot_rect()` 的 top/height 依赖 `legend_entries()` 的图例行数。关闭"参考 Q / 阻容分界线"后图例条目减少 → `plot_rect` 几何变化；但 `set_display_state()` 只做 `_invalidate_base_only()`，没有使当前 Q 路径（`_cur_path`）与 semi pixmap 失效。
且旧缓存键只含 `(Q, K)` 或 `(width, height, Q, K)`，不含绘图区几何。于是当前 Q 曲线仍按旧 `plot_rect` 像素几何画，工作点却由 `_draw_overlay()` 按新 `plot_rect` 实时算 → 二者错位。

**修复**：
- 统一几何版本号 `_geom_gen`：任何改变 `plot_rect` 几何的状态变化都会递增并作废所有路径/pixmap。
- 当前 Q 路径缓存键 = `(Q, K) + _geom_key(r)`，其中 `_geom_key(r)` 覆盖 `r.left / r.top / r.width / r.height / ymax`。
- semi pixmap 缓存键同样以 `_geom_key(r)` 为前缀（`(geom_key, Q, K)`）。
- 只重建像素路径/缓存，**不重算** LLC 数学数据。

**像素误差测试**：新增 `workpoint_on_current_curve_px()` 公开方法，读取当前曲线实际渲染像素 Y 与工作点映射像素 Y 的差值。
覆盖 `K=2.5/5/8 × Q=0.2/0.5/2 × fn=0.4/0.6368/1/2`，关闭两个图层后断言 **≤ 1 px**。

---

## 2. BUG2：拓扑 / 次级整流全部中文化

只改 UI 显示文本，内部 key（`half/full`、`ct_diode/ct_sr/fb_diode/fb_sr`）不变，不影响计算逻辑。

| 控件 | 显示文本 |
|---|---|
| 拓扑 | 半桥、全桥 |
| 次级整流 | 中心抽头二极管整流、中心抽头同步整流、全桥二极管整流、全桥同步整流 |
| 阻容分界线 checkbox | 阻容分界线（已去掉"∠Zin=0"） |

`BRIDGE_OPTIONS` / `RECT_OPTIONS` 均为 `(显示中文, 内部key)` 元组，下拉框渲染中文、信号回调仍按内部 key 计算。

---

## 3. BUG3：阻容分界线命名 + 图形 + Hover

**3.1 命名**：checkbox/图例/图中标签统一只显示"阻容分界线"，物理判据 `∠Zin=0` 仅保留在代码注释与右侧相位/区域判断中。

**3.2 完整画法**（含曲线段 + (1,1) + 竖段）：
- 保留原品红虚线弯曲段（`fnp` 附近 → 逐渐连接 `(fn=1, M=1)`，不留缺口）；
- **新增 fn=1、M=1→0 的竖直段**，与弯曲段同颜色/同虚线风格/同线宽；
- 竖直段不生成第二个图例条目。

**3.3 显示开关**：关闭"阻容分界线"同时隐藏弯曲段、竖直段、图例、图中标签、Hover 命中；后台 `fn_boundary` 与区域判断/工程设计仍继续计算。

**3.4 Hover**：fn=1 竖段不是单值函数 M(fn)，`boundary_gain(fn)` 无法表示。新增独立几何 hit test `_vertical_boundary_hit()`：判断鼠标到 `x(fn=1)` 像素距离且 `mouse_y` 落在 M=0~1 对应像素范围，满足 `HOVER_TOL_PX` 即命中"阻容分界线"，不伪造 `boundary_gain(1)`。

---

## 4. BUG4：工程参数键盘输入自动生效

**修复**：`setKeyboardTracking(False)` 下键盘编辑不再需要失焦/Enter 才能提交。
- 新增单次 debounce timer：**300 ms**（`ENG_DEBOUNCE_MS`）。
- 键盘 `textEdited` → 显示"参数有未完成输入"并启动 debounce；停止输入 300ms 后 `interpretText() → 校验全部工程输入 → 全部有效才 commit → 自动重新计算`。
- Enter/失焦（`editingFinished`）立即提交，不等 debounce。
- 上下箭头/滚轮仍立即更新。
- 输入未完成/暂时非法时保留上一套有效结果，不疯狂报错；仅轻量提示"已自动应用 / 参数有未完成输入"，无笨重保存系统。

**验收**：QTest 键盘模拟直接在 `Vin_min` 输入 `320`，不切换焦点，等待 debounce 后断言设计状态已更新为 320（场景 E 已 PASS）。同时覆盖 Vo / Pout / Vf / η / 手动 n。

---

## 5. BUG5：右侧结果区精简 + 滚动保持

**5.1 信息层级**：
- 默认【关键结果】优先显示：K/Q/fn/M(fn)/fs、n/Lr/Lm/Cr、M_req_min/max、Q_full/overload、fn_min/max、fs_min/max、最坏电流 Ioe/Im/Ir、Cr Irms/Vpeak；
- 【分析】只显示最重要 4~6 条（✓/⚠/✕）；
- 【建议】最多 3~5 条；
- 大量辅助诊断（fnp 详细数值/频率、M(fnp)、fnr 说明、fn_peak、boundary 详细、完整 stress 中间量等）移入可展开的"**详细信息 ▼**"区域，默认折叠。计算能力不删除。
- 默认右侧文字量显著减少（约减少 60%+），关键信息前置到顶部。

**5.2 滚动保持**：更新 `setPlainText` 前快照垂直滚动位置、是否贴底、水平滚动位置；更新后按"原在中间→恢复原位置 / 原在底部→继续贴底 / 原在顶部→继续顶部"恢复。K/Q/fn/Vin 变化均不把用户拉回顶部（场景 F PASS）。

**5.3 拖动节流**：滑块拖动期间不每帧 `setPlainText` 整份长文本；曲线/顶部状态实时更新，完整工程结果/分析/建议按 `RESULT_THROTTLE_MS=120ms` 节流，松手（`sliderReleased`）立即最终刷新。

---

## 6. BUG6：K 滑块性能专项优化

### 6.1/6.2/6.3/6.4 措施
- **隐藏图层懒重建**：`show_reference==False` 时 K 拖动只置 `reference_family_dirty`，不重算 9 条参考族显示数据/路径；`show_boundary==False` 时不重建 420 点 boundary path（标量 `fn_boundary` / `input_region` 数学判据继续算）。
- **工程计算解耦**：K 拖动（`dragging`）完整 `compute_design` / `fn root solve` / stress / analysis 按 100~150ms 节流；`sliderReleased` 立即精确最终计算。
- **长文本不参与 K 每帧重绘**（见 §5.3）。
- **preview 两级采样**：`sliderPressed` → preview 模式，参考族路径降到 ~1/4 采样点（3000→750）；`sliderReleased` → 恢复全精度，只重建一次最终路径。数学峰值/边界/Hover 精度不受影响。

### 6.5 缓存正确性
数学键 / 几何键 / 显示键三者分开，`_geom_key` 覆盖绘图区几何，未因提速重新引入 BUG1 的 stale geometry 问题（相关回归测试全过）。

### 6.6 性能验收（`scripts/bench_k_drag.py`，各 1000 次 K 更新）

| 指标 | 优化前 | 优化后 A(参考Q ON/边界 ON) | 优化后 B(参考Q OFF/边界 OFF) |
|---|---|---|---|
| _do_update median | ~35 ms | **17.57 ms** | **5.31 ms** |
| p95 | — | 23.27 ms | 9.10 ms |
| max | — | 47.39 ms | 14.75 ms |
| family display rebuild | ON | 1000（期望，常显参考） | **0** |
| boundary display rebuild | ON | 1000（期望，常显边界） | **0** |
| engineering solve | ~1000 | **21** | **21** |
| result text rebuild | ~1000 | **21** | **21** |

关键证明：两个可选图层 OFF 时，K 连续拖动中 family/boundary 显示重建 ≈ 0，engineering solve 远低于 K valueChanged 数（21 << 1000）。

---

## 7. BUG7：EXE 体积瘦身（27.65 → 20.84 MB）

| 指标 | 优化前 | 优化后 | 变化 |
|---|---|---|---|
| 大小 | 27,649,756 bytes | 21,854,288 bytes | **-5.53 MB（-21%）** |
| SHA256 | — | `645BDBDC...ECC6` | |

### 7.1 Qt6Network 来源分析
用本地 `Analysis-00.toc` / `xref-*.html` 依赖图查明：**真正入口是 `PySide6/__init__.py` 的延迟/条件导入**（第 89–94 行 `try: from . import QtNetwork`，`QSslSocket.supportsSsl()`），xref 标注 `PySide6.QtNetwork → imported by PySide6 (delayed, conditional, optional)`，被正确判定为可安全剔除。不是 QtUIOTouchPlugin 拉入（该插件也是被过滤项）。最终 EXE 确认**不含** `Qt6Network.dll`。

### 7.2~7.6 剔除清单（每类均经"构建→启动→操作 GUI→导出 PNG→自动测试"验证）
`_BIN_DROP` 按目标路径定向剔除，不手工删 DLL、构建可复现：
- **网络**：`qt6network.dll`（由 PySide6 顶部条件导入拉入）
- **SVG**：`qt6svg.dll`、`qsvgicon.dll`（iconengines）、`qsvg.dll`（imageformats）
- **generic**：`qtuiotouchplugin.dll`（触屏输入，依赖 Qt6Network）
- **imageformats**：`qjpeg/qwebp/qtiff/qgif/qicns/qico/qtga/qwbmp/qdds/qsvg`（程序不加载任何此类图片，PNG 编解码内置于 Qt6Gui.dll）
- **platforms**：只保留 `qwindows.dll`，剔除 `qdirect2d/qminimal/qoffscreen`（仅测试环境需要）
- **styles**：`qmodernwindowsstyle.dll`
- **软件 OpenGL**：`opengl32sw.dll`、`libEGL.dll`、`libGLESv2.dll`、`d3dcompiler_47.dll`（纯 QPainter 栅格绘制不触发 OpenGL）
- **翻译**：整套 96 个 `translations/*.qm`（程序无 QTranslator）
- **加密**：`libcrypto-1_1.dll`（由 stdlib `hashlib` 静态链拉入，`random` 实际用内置 `_sha512`，冻结运行路径从不触发）
- **spec excludes**：`PySide6.QtNetwork` 及 Qt3D/QUick/Multimedia/Charts 等重型模块

### 7.7 UPX
正式版本继续 **不使用 UPX**（避免杀软误报 + OneFile 启动解压成本），未做大/小版本对比。

### 7.8 体积目标
已安全降到 **20.84 MB**，低于 24 MB 目标，无需列出"不可删最大 20 项"（DL依赖分析表明这些按目标路径剔除后 8 项打包审计测试全过、EXE 真实启动渲染正常，无不可安全删除的次要项）。

**包审计测试**（`tests/test_exe_package_audit.py`，对 onedir `_internal`）8 项全过：
无 `.qm` 翻译、无多余 imageformats、platforms 只剩 `qwindows.dll`、无网络/SVG、无软件 OpenGL、无 libcrypto、无 modern windows style、核心 Qt 二进制仍在（Qt6Core/Gui/Widgets/qwindows/python310/pyside6.abi3/shiboken6）。

---

## 8. BUG8：阻容分界线与 fnr=1 不混淆
- `BOUNDARY_COLOR`（品红，较粗虚线）≠ `FNR_COLOR`（蓝色，细虚线），两者 z-order / 线宽不同；
- fn=1 处同时存在：蓝色 `fnr=1` 竖线（M>1 部分独显）+ 品红阻容分界线 M≤1 部分；M≤1 重合段采用明确 dash phase / 线宽叠加，两条概念均可辨认；
- `test_boundary_vline_and_fnr1_coexist` 断言两路径并存且颜色不同。

---

## 9. 核心恒显规则（未被破坏）
当前 Q 曲线、并联谐振 `fnp`、串联谐振 `fnr=1`、当前工作点 fn、当前 Q 曲线增益峰值 始终显示。
仅 `预设参考 Q 曲线 / 阻容分界线 / Mmin~Mmax / fnmin~fnmax` 可开关。未新增 checkbox。

---

## 10. GUI 验收（场景 A–G）

驱动脚本 `scripts/accept_scenarios.py`（真实 Qt 事件循环 + 真实 QPainter 渲染 + 真实信号/布局，`widget.grab()` 导出位图；EXE 真实启动/渲染/resize 由 `scripts/verify_exe.py` 单独验证）。

| 场景 | 内容 | 结果 |
|---|---|---|
| A | K=5, Q≈0.5, fn=1，关闭参考Q+阻容边界，工作点在当前 Q 曲线上 | PASS（像素Y差≤1） |
| B | fn≈0.6368 仍在线上 | PASS |
| C | K 5→2.56 拖动后仍在线上 | PASS |
| D | 重开阻容边界：曲线段+(1,1)+fn=1/M=1→0 竖段 | PASS（两段路径并存） |
| E | 键盘直接输入 Vin_min=320 不失焦，右侧已更新 | PASS |
| F | 右滚中下部，移动 fn/K/Q 滚动位置不跳回顶部 | PASS |
| G | 中文下拉框检查 | PASS |

截图（`scripts/` 下）：
- `_sc_A_boundary_ref_off_fn1.png`
- `_sc_B_boundary_ref_off_fn06368.png`
- `_sc_C_boundary_ref_off_K256.png`
- `_sc_D_boundary_on_K256.png`
- `_sc_E_Vin320_autoapply.png`
- `_sc_F_scroll_preserved.png`
- `_sc_G_combobox_chinese.png`

（截图中文以方框显示是 offscreen 渲染环境缺少 CJK 位图字体所致，真实 EXE 已由 verify_exe.py 实机启动确认正常渲染；结构验证以各场景 PASS 断言为准。）

**真实 EXE 启动验证**（`scripts/verify_exe.py`，纯 ctypes 操作真实冻结进程）：
```
OK: main window found: 'LLC 谐振变换器交互式多增益曲线'
OK: survived resize/repaint
```

---

## 11. 新增测试（覆盖每项 BUG）

`tests/test_regression_fixes.py`：
`test_display_toggle_keeps_workpoint_on_current_curve`
`test_plot_rect_change_invalidates_current_geometry`
`test_semi_cache_key_tracks_plot_geometry`
`test_boundary_contains_vertical_fn1_segment`
`test_boundary_vertical_segment_hover`
`test_boundary_ui_label_is_chinese_without_angle_text`
`test_boundary_vline_and_fnr1_coexist`
`test_topology_combobox_chinese_labels`
`test_rectifier_combobox_chinese_labels`
`test_spinbox_keyboard_auto_commit_without_focus_loss`
`test_result_scroll_position_preserved`
`test_hidden_reference_skips_k_family_display_rebuild`
`test_hidden_boundary_skips_display_boundary_rebuild`
`test_k_drag_throttles_engineering_recompute`
`test_k_drag_throttles_long_result_text_rebuild`

`tests/test_exe_package_audit.py`（8 项打包审计，见 §7.8）。
`tests/test_perf_fix.py` 增补 preview 降采样测试。

**pytest 结果：1141 passed, 0 failed**（含原 1112 项 + 新增回归）。

---

## 修改文件 / 新增脚本

**修改**：`LLC_Gain_Curve.spec`、`src/main.py`、`src/plot_widget.py`、`src/llc_report.py`、`tests/test_boundary_gui.py`、`tests/test_engineering_gui.py`、`tests/test_gui_smoke.py`、`tests/test_perf_fix.py`

**新增**：`tests/test_regression_fixes.py`、`tests/test_exe_package_audit.py`、`scripts/bench_k_drag.py`、`scripts/accept_scenarios.py`

---

## 交付产物
- 新 EXE 绝对路径：`F:\OneDrive\找工作\电源\LLC\LLC增益曲线_EXE\dist\LLC增益曲线.exe`
- 大小：`21,854,288 bytes`（20.84 MB）
- SHA256：`645BDBDC97A8AAD2FC05D957B975BC0B561E733BBA3544C307AEC92C684EECC6`
- 本地 commit：`1f193f6`（已提交本地，未 push GitHub）