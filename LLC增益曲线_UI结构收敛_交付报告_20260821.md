# LLC 增益曲线 — UI 结构收敛 + 单一滚动结果区 交付报告

日期：2026-08-21　版本：本轮 UI 结构收敛（基于 v4）
项目目录（唯一工作目录）：`F:\OneDrive\找工作\电源\LLC\LLC增益曲线_EXE`
Git 状态：**未 git commit，未 push GitHub**（按要求保留，等待用户本人验收）

---

## 0. 本轮实际修复的两个"已存在但仍会崩溃/失效"问题

进入本轮后先对当前 v4 源码做了真机核对，发现 UI 结构收敛代码虽然已写入
`src/main.py`，但有**两个致命缺口会让"工程参数设置"与"Fluent 皮肤"完全失效**，
本轮已实质性修掉（不是零散补丁）：

1. `_section()` 内 `if len(spec) == 3:` 分支判断错误 —— 每个字段元组实际是
   `(label, control, r, c)`（4 个元素），却走 `len==3` 分支判断，导致所有工程
   参数字段进入"6 元素拆包"的 else 分支 → 启动即抛
   `ValueError: not enough values to unpack (expected 6, got 4)`，
   **工程参数设置整块无法构建、GUI 一起崩溃**。已改为 `len(spec) == 4`。
   （这是此前 100 个 collection ERROR 的根因。）

2. `APP_QSS`（需求九的 Windows 11 / Fluent 全局皮肤）**只在文件中定义、
   从未 `setStyleSheet` 应用** → 滑块/输入框/ComboBox/滚动条/卡片全部停留在
   Qt 原生复古外观。已在 `main()` 中 `app.setStyleSheet(APP_QSS)` 真正应用。
   （这是截图里"现代 Fluent 外观"能生效的前提。）

---

## 1.「计算模型：FHA」—— 彻底删除 ✅

源码 `src/main.py` 中不再存在模型提示标签；经全标签文本扫描 + QSS 扫描 +
状态栏扫描均无 `计算模型` / `FHA`。工程参数区卡片标题只叫 **工程参数设置**。
`test_no_model_hint_label` 回归覆盖。

## 2. 工程参数布局 —— 字段卡式，恢复横向空间 ✅

- 取消旧的 3 列 `[label][control][addStretch]` 实现。
- 每个字段改为 **label 在上、control 在下** 的字段卡（`field()` 工厂：上下 6px，
  字段间 12px，组间 20~24px，卡片内边距 12px，统一 4px spacing 体系）。
- 划分为四个逻辑 mini-section：**输入规格**（Vin_min/nom/max）、**输出规格**
  （Vo/Pout/Io）、**拓扑与变压器**（拓扑/匝比模式/次级整流/n）、**设计参数**
  （η/Vf/Q 模式/过载）；"次级整流"ComboBox 跨两列（427px 实测）。
- 控件 `QSizePolicy.Expanding` 随字段扩展，不再 `setMaximumWidth(125)` 后左边堆放。
- **右侧大片空白为何消失**：所有输入控件宽度随卡片/栅格自动扩展（实测最窄列控件
  ~207px，最长 ComboBox 427px）；1920 宽下工程区以 4 个等宽 mini-section 并排铺满
  （engPanel≈1452px），字段填充整块宽度，无整行 stretch。
- 真机响应式：`_relayout_eng()` 依据 `engPanel.width()` 动态切 **1920→4 列并排**、
  **1440/1280→2×2**；无水平滚动条。实测：1920 四卡同排（y 均=0），1440 与 1280
  为 2×2（engPanel<1020 触发）。
- 回归：`test_engineering_fields_use_expanding_width`、`test_no_large_unused_engineering_panel_space`。

## 3.「恒显：当前 Q / fnp / ...」—— 彻底删除 ✅

全标签文本 + QSS 均无 `恒显`；无 Tooltip、无说明文字替代。
`test_no_always_visible_note` 覆盖。

## 4. 显示选项 —— 紧凑 2×2，不再占用大块高度 ✅

面板只有 4 个勾选框，2×2 排列（正好 4 个，实测）：
`☑ 预设参考Q曲线 ☑ 阻容分界线 / ☐ Mmin~Mmax范围 ☐ fnmin~fnmax范围`。
- 删除 `恒显...` 文字、删除 `addStretch(1)`。
- 卡片 `QSizePolicy.Preferred / Maximum`，高度只由两行内容决定。
- 与工程参数区顶部对齐（`eng_row.setAlignment(disp_panel, Qt.AlignTop)`）。
- 回归：`test_display_panel_has_no_stretch_blank`（断言无任何可拉伸 spacer + 垂直 Maximum）。

## 5. 右侧结果 —— 单一滚动区域 ✅（本轮最重要的修复）

删除旧 `detailBox = QPlainTextEdit(...)` 与旧 `ResultCardsView` 及其
`_container.setMinimumHeight(total_h)` 手工高度 hack。重写为单一 **`ResultPanel(QScrollArea)`**：

```
QScrollArea(唯一)
 └ resultContent(QWidget)
    └ resultLayout(VBox, AlignTop, SetMinAndMaxSize, 无 addStretch)
       ├ card 当前工作点 / 谐振腔参数 / 调频范围 / 电流与应力 / 设计分析 / 建议
       └ detailToggle「详细信息 ▸」+ detailFrame（同一滚动内容，默认折叠）
```
- 整列 **只有一根垂直滚动条**；`detailFrame` 是同一 resultContent 的 section，
  展开后仍在**同一个**滚动区，绝无第二个滚动条。
- 实测（含真实 EXE 布局 + offscreen 断言）：右侧 `findChildren(QScrollArea)`
  除自身外为 **0**；「详细信息」展开后仍在同一滚动内容里。
- 回归：`test_result_panel_has_single_scroll_area`、`test_detail_is_inside_same_result_scroll`。

## 6. 右侧"整块空白"修复 + 启动即显示 ✅

根因：旧 `ResultCardsView` 用 `frame.sizeHint()` 求和塞进
`_container.setMinimumHeight(total_h)`，在不同 Qt platform style / DPI / 真机
Windows 下尺寸计算失效 → 上半区大块空白、下半区又冒出第二个详情滚动区。
本轮**废弃该 hack**：尺寸交给 `QLayout.SetMinAndMaxSize` + Qt 依据内容自动维护，
`resultLayout.setAlignment(Qt.AlignTop)`，无 `addStretch(1)`，无手工 minHeight。

"启动后立即显示"保证：6 张卡片在 `MainWindow` 构造时即创建并加进
resultContent，默认可见、无需 show/滚动。真实 EXE 默认启动截图首屏即显示
**当前工作点**（K/Q/fn/M(fn)/fs）与 **谐振腔参数**。
回归：`test_result_panel_has_content_immediately_after_startup`、
`test_result_layout_aligns_top`、`test_result_panel_has_no_manual_minheight_hack`。

## 7.「输入后 300ms 自动生效」—— 文案删除 ✅

参数调节区当前 Q 提示改为「（对数，0.05 ~ 10.0）」，只保留范围；内部 debounce 仍
为 300ms（`_q_debounce.interval()==300`），UI 不再展示实现细节。
回归：`test_no_q_300ms_hint`。

## 8. 门头（院校 + 姓名）字号 ✅

`centerLabel`（西安电子科技大学 张名扬）与 `statusLabel`（右侧 K/Q/fn/M(fn)/fs/fr）
**字号一致，均为 11pt**；`centerLabel` 用 `QFont.Medium`（Semibold），
`statusLabel` 用 Regular。回归：`test_center_label_same_font_size_as_status`。

## 9. 统一 QSS（Windows 11 / Fluent），现已实际应用 ✅

本轮把 `APP_QSS` 从"只定义"改为"真正 `setStyleSheet`"。修改涵盖：
- **Slider**：4px 轨道 `#E2E8F0`、`sub-page` 蓝色 `#2563EB`、16px 白色圆形手柄带
  2px 蓝色描边，hover 变 `#DBEAFE`。（不改数学映射）
- **QDoubleSpinBox / ComboBox**：统一 `min-height:30px`、白底、`#CBD5E1` 描边、
  6px 圆角、focus `#2563EB`；下拉项 hover `#F1F5F9`、选中 `#DBEAFE`。
- **Scrollbar**：宽 10px、透明背景、handle `#CBD5E1`（hover `#94A3B8`）、圆角 5px、
  `sub/add-line height:0` 去三角。
- **Card（替代 QGroupBox）**：`QFrame#card` 白底 + 1px `#E2E8F0` 描边 + 8px 圆角，
  `#subCard` 6px 圆角；标题用 `QLabel#cardTitle`；参数调节/工程参数设置/显示选项
  全部是 Card+标题，不再用老式 QGroupBox 线框+title 缺口。
- 配色：窗口 `#F6F8FB`、主文字 `#0F172A`、次级 `#64748B`、边框 `#E2E8F0`、
  强调 `#2563EB`、成功 `#16A34A`、警告 `#D97706`、错误 `#DC2626`。曲线配色未动。
- 回归：`test_engineering_panel_renamed` 顺带断言**不再有 QGroupBox**（`==0`）。

## 10. 结果 Card 美化 + 分析颜色 ✅

- 6 张固定顺序卡片：当前工作点→谐振腔参数→调频范围→电流与应力→设计分析→建议→
  详细信息（默认折叠）。
- `_ResultCard` 用 **QGridLayout `label|value`**（名称 `resultName` 次级色、值
  `resultValue` 加粗深色），不再是 `"Re = 96.42 Ω"` 拼字符串。
- 设计分析 **✓绿 / ⚠橙 / ✕红**（`_FLAG_COLOR`），只给标记着色，不整卡变红。
- `build_result_cards()` 是唯一数据源，`build_result_sections()` 兼容压平文本；
  参数变化只更新 value QLabel，**card 对象身份不变**。
- 回归：`test_result_card_identity_stable_before_after`。

## 11. 工程参数状态提示 ✅

「已自动应用」状态小字保留，但只占**标题行右侧**（不与标题抢整行），不产生额外
底部空行；标题行 `QLabel#engStatus`（`#16A34A`）。输入中显示"参数有未完成输入"。
回到本例：首帧 `engStatusLabel=""`，不占空间。

## 12. 显示选项与工程参数高度关系 ✅

`disp_panel.setSizePolicy(Preferred, Maximum)` 且 `eng_row` 顶部对齐 —— 显示选项
高度只由 4 个勾选框决定，不强行拉伸到与工程参数等高。回归 `test_display_panel_has_no_stretch_blank`。

## 13. 响应式：1440×960 / 1920×1080 / 1280×800 ✅

真机实测（`_relayout_eng` 几何探针）：
- 1920×1080：工程区 4 mini-section 并排铺满（engPanel≈1452px，y 全=0）。
- 1440×960 / 1280×800：工程区 2×2（engPanel=972 / 812 < 1020 触发），无水平滚动条。
- 窗口最小尺寸 1040×820；右侧结果面板宽度 340~430（随窗口自适应）。
- 三档分辨率截图均完成真机 PrintWindow 采集（见 §15）。

## 14. 数学与绘图功能未动 ✅

仅调整 GUI 结构 / 结果展示 / 布局 / 样式。当前 Q 曲线、参考 Q 曲线、阻容分界线、
fnp、fnr=1、工作点、峰值、Hover、M 范围、fn 范围、Re/Lr/Lm/Cr、fn solver、
FHA stress、TI regression 全部保持，未改任何数学公式。全量 1156 项功能/数学/绘图
测试全部通过（另有 8 项 EXE 打包审计测试因未保留 onedir 中间目录而按设计跳过，
构建 onedir 后为 1164 全绿）。

## 15. 真实 Windows 真机验收 ✅

- 启动真实 `dist\LLC增益曲线.exe`，`verify_exe.py` 确认主窗口出现（
  'LLC 谐振变换器交互式多增益曲线'）并撑过 resize/重绘 ✓。
- 用真实 EXE 以 PrintWindow 采集窗口：
  - 默认启动 `_accept\sc1_default.png`（1434×998）：右侧首屏立即显示
    当前工作点 + 谐振腔参数，非空白。
  - `_accept\res_1440x960.png`（1440×960）
  - `_accept\res_1920x1080.png`（1920×1080）
  - `_accept\res_1280x800.png`（1280×858，受窗口最小 820 高限制，达到硬件允许最小值）
  - `_accept\detail_expanded_1440.png`：详情展开，仍在同一滚动区，右侧仅一根滚动条
- 工程参数/显示选项字段实测全部有值、有真实几何（Vin 300/390/480、Vo 12、Pout 600、
  Io 50、n 15.6、η 0.92、Vf 0.5、拓扑=半桥、次级整流=中心抽头同步整流(427px)、
  Q 模式=手动、过载=110%）。

## 16. 自动测试

- 新增结构化回归文件 `tests\test_ui_structure.py`，含需求十六所列 12 项 + 卡片身份
  稳定性，共 13 项。
- 同步修正 8 个引用已删除 API（`detailBox`/`ResultCardsView`/QGroupBox）的旧测试，
  迁移到新 `ResultPanel` + 单滚动区 + `detailText`。
- 全量：**1156 passed, 0 failed, 8 skipped**（8 项为 EXE 打包审计，因未保留 onedir
  目录跳过；保留 onedir 时为 1164 全绿）。

## 17. 交付物

| 项 | 值 |
|---|---|
| 可执行文件 | `F:\OneDrive\找工作\电源\LLC\LLC增益曲线_EXE\dist\LLC增益曲线.exe` |
| 大小 | **20.85 MB**（< 24 MB） |
| SHA-256 | `B4A9B7C2E1E5DCC36CDBDEE7590D5B3BD6099E8A0A6D55BDE317B119382C6E8A` |
| 截图 | `_accept\` 目录（4 张真机 + 1 张展开详情） |

## 18. 本轮重验 + 权威几何确认（2026-08-21 终版）

- **重新构建 EXE**：清理被 OneDrive/残留进程锁定的 `build` 目录后重建成功，
  新文件 `dist\LLC增益曲线.exe`，大小 20.85 MB，SHA-256 `B4A9B7…6E8A`
  （PyInstaller 非确定性，哈希每次构建略变，体积一致）。
- **重新真机验收**：真实启动新 EXE，`verify_exe.py` 确认主窗口
  `LLC 谐振变换器交互式多增益曲线` 出现并撑过 resize/repaint。
- **权威几何探测（真实 Windows QPA，非 offscreen）**，避免只依赖离屏：
  - 整窗 `findChildren(QScrollArea)` **= 1**（唯一右侧滚动区），结果竖向滚动条可见，
    「详细信息」展开后仍在同一滚动内容（回归 `test_detail_is_inside_same_result_scroll`）。
  - 首屏 6 张卡片全部可见（当前工作点在前），K/Q/fn/M(fn)/fs 非空。
  - 三档分辨率工程区均为 4 mini-section 单行并排（rows `[(0,0),(0,1),(0,2),(0,3)]`），
    engPanel 底部 ≤ 812，客户端 820 内**无任何输入控件越界**（最底 spinbox bottom=785）。
  - 1280×800 下窗口按 `setMinimumSize(820)` 自动夹到最小高度（外框 858 为设备像素，
    125% 缩放），工程参数输入框与显示选项均完整、不被裁切。
- **新截图目录**：`_accept_v6\`（`sc1_default` / `res_1440x960` / `res_1920x1080` /
  `res_1280x800` / `detail_open_1440`）。

## 19. Git

**本轮未 git commit；未 push GitHub。** 构建/测试/验收全部在本机该目录内完成，
等待用户亲自打开 EXE 验收后，再决定是否提交。