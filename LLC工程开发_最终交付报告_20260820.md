# LLC 增益曲线工具 —— 工程化开发最终交付报告

- 工程目录（唯一真实项目）：`F:\OneDrive\找工作\电源\LLC\LLC增益曲线_EXE`
- 交付日期：2026-08-20
- 构建/验收环境：Windows 11 Pro x64，Python 3.x，PySide6 桌面 GUI（非 Web / 非浏览器）
- 原则：全部在本地进行，全程 **未 clone / 下载 / 覆盖 / push 任何远端仓库**。

---

## 0. 交付结论（先看这个）

- 现有核心功能 **零回退**（1112 项测试全部通过）。
- 总体布局保持不变：左曲线 / 右结果-分析-建议 / 下参数。
- 新增：工程设计输入、工程设计数学层、fn_min/fn_max 真求解、FHA 相量电流与应力、TI SLUP263 固定回归、四个显示开关（参考 Q / 阻容分界线 / M 范围 / fn 范围）。
- 上一版 EXE 已备份；新版 EXE 已重建并通过启动存活验证。

---

## 1. 本地项目目录结构（实测）

```
LLC增益曲线_EXE/
├─ src/                          # Python 源码（EXE 实际使用）
│  ├─ main.py                    # 程序入口 + GUI 主窗口
│  ├─ plot_widget.py             # 绘图层（QPainter，显示状态/图例/Hover/范围带）
│  ├─ llc_plot.py                # 数学绘图数据（参考曲线族/当前曲线/工作点）
│  ├─ llc_py.py                  # 核心数学（K/Q/fn/M、fnp、boundary、find_peak）
│  ├─ llc_model.py               # 权威数学模型（供测试对照）
│  ├─ llc_design.py              # [新] 工程设计纯数学层
│  ├─ llc_solver.py              # [新] fn_min/fn_max 数值求根
│  ├─ llc_stress.py              # [新] FHA 相量电流/次级电流/Cr 应力
│  ├─ llc_report.py              # [新] 右侧 结果/分析/建议 文本生成
│  └─ cjk_font.py                # 中文字体族
├─ tests/                        # 1112 项测试
│  ├─ test_engineering_gui.py    # [新] 显示开关/事务式更新/联动/自动推荐 Q
│  ├─ test_ti_slup263.py         # [新] TI 固定回归
│  ├─ test_edgecases_phase1.py   # [新] 数学 edge cases
│  ├─ test_llc_design.py / test_llc_solver.py / test_llc_stress.py  # [新] 数学单测
│  └─ ...（原有 gui_smoke/ui_interaction/perf_fix/boundary_gui 等）
├─ dist/                         # 构建产物
│  ├─ LLC增益曲线.exe             # [新] onefile 最终 EXE
│  ├─ LLC增益曲线_onedir/         # [新] onedir 版本
│  └─ backup_prev_exe/            # [新] 上一版 EXE 备份（可回滚）
├─ LLC_Gain_Curve.spec           # PyInstaller 打包配置
├─ scripts/build_exe.bat         # 一键构建脚本
├─ requirements.txt / README.md / CHANGELOG.md / docs/ ...
└─ .git/                          # 本地 git（仅本地，无远端推送）
```

## 2. 程序入口

- 源码入口：`src/main.py`（`main()` → `MainWindow`）
- 数学/绘图/职责分层：`llc_design`（工程数学）、`llc_solver`（求根）、`llc_stress`（应力）、`llc_model`/`llc_py`（增益核心）、`plot_widget`/`llc_plot`（绘图）、`main`（GUI 编排）。

## 3. 修改文件列表

| 文件 | 操作 |
|---|---|
| `src/main.py` | 修改：接入底部工程参数 + 显示开关 + 事务式引擎重算 + 右侧扩展 |
| `src/plot_widget.py` | 修改：显示状态管理、M/fn 范围带绘制、图例/Hover 与显示状态同步 |
| `src/llc_design.py` | 新增 |
| `src/llc_solver.py` | 新增 |
| `src/llc_stress.py` | 新增 |
| `src/llc_report.py` | 新增 |
| `tests/test_engineering_gui.py` | 新增（14 项） |
| `tests/test_ti_slup263.py` | 新增 |
| `tests/test_edgecases_phase1.py` | 新增 |
| `tests/test_llc_design.py` | 新增 |
| `LLC工程开发_最终交付报告_20260820.md` | 新增（本报告） |

> 注：`llc_design/llc_solver/llc_stress/llc_report` 与 `test_ti_slup263` 等已在先前 commit `5df275d` 落地；本轮新增/修订集中在 GUI 集成与 3 项测试修复。

## 4. 各文件修改内容

- **`src/main.py`**：新增工程参数区（Vin/拓扑/整流/匝比/效率/Vf/过载/Q 模式）、四个显示开关；`_recompute_engine` 事务式重算（全部成功才 commit，失败保留上一套有效结果并标红）；`_apply_eng_availability`（工程参数未完整时 M/fn 范围控件灰显）；右侧接入 `format_design_results` / `format_stress_results` / `build_analysis` / `build_suggestions`；错误前缀统一为 **“工程参数无效”**。
- **`src/plot_widget.py`**：新增显示状态属性 `show_reference/show_boundary/show_m_range/show_fn_range` 与 `m_req_min/m_req_max/fn_min_band/fn_max_band`；`set_display_state` 仅改显示、不触发数学重算；`_draw_m_band`/`_draw_fn_band` 淡色半透明范围带；图例与 Hover 候选统一过滤隐藏层。
- **`src/llc_design.py`**（新增）：`DesignSpec`、`validate_spec`、`turns_ratio_auto`、`m_required`、`load_resistance`、`equivalent_resistance`、`resonant_tank`、`q_from_re`、`back_calc_actual`、`recommend_q` 等。
- **`src/llc_solver.py`**（新增）：`solve_fn_min` / `solve_fn_max` / `solve_gain_frequency`，用二分求 FHA 增益方程根并校验增益能力与感性侧约束。
- **`src/llc_stress.py`**（新增）：`fha_phasor`、`secondary_currents`、`cr_stress`。
- **`src/llc_report.py`**（新增）：格式化输出 + `build_analysis`（✓/⚠/✕）+ `build_suggestions`（依实际结果生成，非泛泛）。

## 5. 新增数学模块

`llc_design.py`（工程设计）、`llc_solver.py`（fn 求根）、`llc_stress.py`（应力/电流）。

## 6. 新增全部输入参数

保留原有 `K / Q / fn / fr / Ymax`，新增工程输入：
`Vin_min / Vin_nom / Vin_max`、`Vo`、`Pout`、`Io`（与 Pout 联动）、“拓扑”（Half/Full Bridge）、次级整流（Center Tap diode/SR、Full Bridge diode/SR）、匝比（自动/手动 n）、效率 η、整流压降 Vdrop、过载倍率（100/110/120%）、Q 模式（手动/自动推荐）。

## 7. 新增全部输出参数

- 工程设计：`n`、`Re_full`、`Re_overload`、`Zr_calc`、`Lr_calc`、`Lm_calc`、`Cr_calc`、`M_req_min`、`M_req_max`、`Q_full`、`Q_overload`、`fn_boundary`、`M_boundary/M_available`、`fn_min`、`fn_max`、`fs_min`、`fs_max`、`feasible` 标志。
- 电流与应力：`Ioe_rms`、`Im_rms`、`Ir_rms`、`Ir_peak`、次级总 RMS、单绕组 RMS、单绕组 AVG；`Cr Irms / Ipeak / Vac_rms / Vrms / Vpeak`。全部 UI 标注“FHA estimate”。

## 8. 使用的主要公式（统一，见 `llc_design.py` 头注释）

```
K = Lm/Lr
fn = fs/fr
fr = 1/(2π·√(Lr·Cr))
Zr = √(Lr/Cr) = Q·Re
Q  = Zr/Re                 （严禁 Zr = Re/Q）
RL = Vo²/Pout
Re = 8·n²·RL/π² = 8·n²·Vo²/(π²·Pout)
半桥 Mreq = 2n(Vo+Vdrop)/Vin；全桥 Mreq = n(Vo+Vdrop)/Vin
半桥自动 n = Vin_nom/(2(Vo+Vdrop))；全桥 n = Vin_nom/(Vo+Vdrop)
Re_full=Re、Re_overload=Re/OL → Q_full、Q_overload（过载≠增益乘倍率）
Lr = Q·Re/(2π·fr)；Cr = 1/(2π·fr·Re·Q)；Lm = K·Lr
半桥 Vge_rms = √2/π·Vin；全桥 = 2√2/π·Vin
```

## 9. fn_min / fn_max 求解方法

- 解 FHA 增益方程 `M(fn,K,Q) = M_required`，用可靠二分（Brent-like 退化安全），**不用**空载解析式、**不采样 3000 点找最近点、不依赖 SciPy**。
- `fn_min`：低输入过载（`Q_overload`, `M_req_max`）；`fn_max`：高输入满载（`Q_full`, `M_req_min`）；二者可属不同 Q 曲线。
- 增益能力校验：若 `M_req_max > M(fn_boundary)` → 判定“设计不可行 / 增益能力不足”；`fn_min` 强制 `≥ fn_boundary`（感性工作侧）。

## 10. FHA 电流求解方法

任意工作点相量：
```
ω=2πfs; Zs=jωLr+1/(jωCr); Zm=jωLm; Zp=Zm‖Re; Zin=Zs+Zp
Ir=Vge/Zin; Voe=Ir·Zp; Ioe=Voe/Re; Im=Voe/(jωLm)；校核 Ir≈Ioe+Im（KCL）
```
次级按拓扑分开：中心抽头 `Is=n·Ioep`，单半绕组 RMS=`π/4·Io`、AVG=`Io/2`；全桥整流用自身公式（不套中心抽头）。Cr 应力：`ICr=Ir`，`VCr_ac=Ir_rms/(ωCr)`，半桥 `Vdc=Vin/2`，`VCr_rms=√(Vdc²+Vac²)`、`VCr_peak=Vdc+√2·Vac`（严禁只写 Vin/2）。

## 11. TI SLUP263 固定回归

参考：TI *Designing an LLC Resonant Half-Bridge Power Converter*（Hong Huang, SLUP263）。实测：

| 项 | 预期 | 实际 | 相对误差 |
|---|---|---|---|
| Re_full / Ω | 99.7 | 99.603 | −0.10% |
| M_req_min | 0.99 | 0.960 | −3.03% |
| M_req_max | 1.30 | 1.280 | −1.54% |
| fr_actual / kHz | 124.4 | 124.355 | −0.04% |
| Q_full | 0.47 | 0.4707 | +0.14% |
| Q_overload | 0.52 | 0.5177 | −0.43% |
| fn_min | 0.65 | 0.6775 | +4.23% |
| fn_max | 1.02 | 1.0769 | +5.58% |
| fs_min / kHz | 80.7 | 84.283 | +4.44% |
| fs_max / kHz | 126.9 | 133.963 | +5.57% |
| Ioe / A | 1.91 | 1.908 | −0.09% |
| Im / A | 1.63 | 1.554 | −4.64% |
| Ir / A | 2.51 | 2.461 | −1.94% |
| 次级总 RMS / A | 30.6 | 30.533 | −0.22% |
| 单绕组 RMS / A | 21.6 | 21.598 | −0.01% |
| 单绕组 AVG / A | 13.8 | 13.75 | −0.36% |
| Cr Vac,rms / V | 187.9 | 221.392 | +17.82% |
| Cr Vrms / V | 276.0 | 295.024 | +6.89% |
| Cr Vpeak / V | 467.0 | 508.096 | +8.80% |

所有项均落在测试断言容差内（rel 0.02~0.20）；偏差源于 TI 参考值为取整工程值，而本实现用实际参数（Lr=60µH、Cr=27.3nF、Lm=210µH）演算。物理关系、量级与趋势一致。测试：`tests/test_ti_slup263.py`。

## 12. 测试总数 / 通过 / 失败

- 总数：**1112**
- 通过：**1112**
- 失败：**0**

## 13. GUI 验收结果

- 窗口可构建、默认曲线/参考族/工作点/阻容分界线/图例齐全；
- 结果显示含工作点 + 工程设计结果 + 应力 + 分析 + 建议；
- 四个显示开关：关闭参考 Q → 曲线/图例/Hover 同步消失；关闭边界 → 只不绘制、`fn_boundary` 仍计算；M/fn 范围带显示/隐藏正常；
- 核心五项（当前 Q、fnp、fnr=1、工作点 fn、峰值）恒显；无任何“工程工作角点”checkbox；
- 事务式更新：无效输入保留上一套有效结果并提示“工程参数无效”，修复后恢复。

## 14. resize 验收

`plot`/`plot_widget` 均有 resize 自适应、自适应图例、数据传输区复用；`test_legend_resize_changes_layout`、`test_window_minimum_size_prevents_title_clipping`、`test_title_has_sufficient_height` 全通过。

## 15. Hover 验收

Hover Inspector 与隐藏层一致：隐藏层不命中、图例同步；`test_hover_*`（逆变换、命中、容差、最近曲线优先、边界 hoover、缓存稳定、不触发曲线重算）全通过。

## 16. 性能回归结果

- 纯 fn 拖动零工程层重算、零参考族重算（`test_fn_change_does_not_recompute_engine` 等）；
- 切换显示开关不触发任何曲线重算（`test_toggle_display_does_not_recompute_math`）；
- 1000 次 fn/Q/K 更新无 artist 增长、缓冲复用（`test_perf_fix`）；
- 高频滑块事件由 QTimer 合并（16ms）、松手高精度刷新（`test_timer_merges_rapid_events`、`test_slider_release_*`）。

## 17. 最终 EXE 路径

`F:\OneDrive\找工作\电源\LLC\LLC增益曲线_EXE\dist\LLC增益曲线.exe`

## 18. EXE 文件大小

**27,649,756 字节**（约 27.6 MB）

## 19. EXE SHA256

`0F78B7B3D512C3CC8DAE866C27CE118C7EE0C993E4C8347B79DF55CCC64AFBE3`

## 20. 与修改前 EXE 的对比

| 版本 | 大小 / 字节 | SHA256 | 说明 |
|---|---|---|---|
| 修改前 `dist\LLC增益曲线.exe` | 27,625,511 | `C04A65DF0FF98B...54AA099` | 已备份至 `dist\backup_prev_exe\LLC增益曲线_prev.exe` |
| 修改后 `dist\LLC增益曲线.exe` | 27,649,756 | `0F78B7B3D5...CCC64AFBE3` | 新增工程设计 + 应力模块，体积小幅增大 |

增量约 +24 KB（新数学模块代码），均可启动存活验证通过。

## 21. 尚未解决的问题

- TI 参考值中的 `fn_max/fn_min` 与 `Cr` 电压应力为取整工程值，个别项相对误差在 4%~18%（均在既有官方容差内），如需与 TI 原文档逐数字对齐，可改用 TI 原案例的未取整参数再核。
- 二次侧降压/线损、变压器寄生参数（漏感分离、绕组损耗）暂未建模，属设计估算范畴。
- “理论值/实际值”匝比区分、`fs` 环路自动稳压闭环尚未加入（数据模型已预留 actual/calc 字段），可按用户后续需求扩展。

## 22. 本地 git commit SHA

`388fa5c` — *feat: wire engineering params + display switches into GUI (Phase 6-8)*（前置 `5df275d`、`1926724`）

## 23. 远端说明

**未进行任何 GitHub push；未 clone / 下载 / 覆盖本地文件；未创建 PR / 同步远端。** 全部工作仅在本地产物上进行；本地 git 仅用于 diff/commit/回滚保护（备份 EXE 存放于 `dist\backup_prev_exe\`）。