# LLC 增益曲线工具 — UI 重构 / Preview 数据隔离 / 交互提升 最终交付报告

- 项目目录：`F:\OneDrive\找工作\电源\LLC\LLC增益曲线_EXE`
- 交付日期：2026-08-21
- 范围：仅修复/优化本轮 12 项（数据隔离 + 命名 + 布局 + 视觉 + 交互性能），未重新设计数学模型、未新增无关大功能、全程未访问/推送 GitHub

---

## 0. 交付基线

| 项目 | 值 |
|---|---|
| 本轮改动文件 | `src/main.py`、`src/plot_widget.py`、`src/llc_report.py`、`tests/test_perf_fix.py`、`tests/test_engineering_gui.py`、`tests/test_gui_smoke.py`、`tests/test_ui_interaction.py` |
| 新增脚本 | `scripts/accept_ui_v2.py`（真实渲染验收） |
| 新增回归测试 | 9 项（`tests/test_perf_fix.py`） |
| 测试结果 | **1150 passed, 0 failed**（原 1141 + 新增 9） |
| 前版本 EXE | 21,854,288 bytes（20.84 MB）SHA256 `645BDBDC...ECC6` |
| 交付 EXE | 21,859,609 bytes（**20.85 MB**）SHA256 `CB939DA42F1465FA09233588F9AF07B81CFA4FF06EC1938FE57DC40E211C25C6` |
| 体积目标 | < 24 MB ✅（保持 20.85 MB） |

---

## 1. BUG1：K 拖动后参考 Q 曲线变彩色色带（数据污染）

**根因**（v3 引入的 preview 优化缺陷）：旧 preview 实现 `_compute_family(preview=True)` 把降采样结果**稀疏写入**正式 `family_y` 数组（`y[j*step]`），未写出的索引残留上一版 K 的旧值；每改一次 K 就有新旧值交错 → 拖动中参考族既有鼠标旧值又有新值 → 渲染成浑浊的宽彩色带。

**修复**：**数据内存彻底隔离**
- 新增独立降采样数组 `family_preview_x / fn2_p / fn2m1_p / family_preview_y`（构造时按 `preview_step=4` 采样，3000 → 750 点）。
- `_compute_family(k, preview=True)` 只写 `family_preview_y`，**绝不触碰**全分辨率 `family_y`；`preview=False` 才完整重算正式 `family_y`。
- `set_preview()` 退出时强制重算一次全分辨率 `family_y`，从源头杜绝拖动期残留。
- `_ensure_static_paths` 以 `preview` 参与路径缓存键，preview 路径读独立数组、正式路径读全采样数组，两者严格对应。
- K 滑块按下/释放（`_on_slider_pressed/_on_released`）以 `sender() is sliderK` 精确绑定 preview 进出，Q/fn 拖动不触碰 preview。

**回归测试**：
- `test_preview_uses_separate_data_array`：preview 更新 K 后正式 `family_y` **逐点不变**，preview 数组与新 K 公式一致。
- `test_preview_hidden_reference_still_pollution_free`：参考族隐藏时同样不得污染。
- `test_exit_preview_recomputes_full_family`：退出后全采样 `family_y` 与公式一致、无残留。
- `test_preview_reduced_sampling_length`：preview 为 ~1/4 采样、数学曲线仍全采样。

> 数学精度不受影响：峰值/边界/Hover 恒用精确公式，仅显示采样降为 1/4（需求 6.4 语义）。

---

## 2. BUG8：顶部增加学校与姓名

`main.py` 顶部新增居中署名标签 `centerLabel = "西安电子科技大学   张名扬"`（独立于标题区间，`AlignCenter`，计入统一视觉字体）。

**回归**：`test_top_banner_shows_school_and_name`（含"西安电子科技大学"与"张名扬"）。

---

## 3. BUG2 / BUG11：工程面板命名与布局

- **BUG2 更名**：工程参数面板 `QGroupBox("工程参数设置")`（原含糊标题），内部参数（Vin/Vo/Pout/Io/拓扑/匝比/η/Vf）统一。
- **BUG11 布局重排**：标签紧贴输入框（同 grid 行，`setFixedWidth/setFixedHeight` 统一高度），控件对齐整齐。

**回归**：`test_engineering_panel_renamed`（`findChildren(QGroupBox)` 含"工程参数设置"）。

---

## 4. BUG5：Q 增加数字输入并双向同步（300ms debounce）

- 新增 `spinQ`（`QDoubleSpinBox`，4 位小数，0.05~10，范围与滑块映射一致）。
- **滑块 → 输入框**：Q 变化时 `blockSignals` 回写 spinQ，防信号环。
- **输入框 → 滑块**：`lineEdit().textEdited` 启动 300ms 单次 debounce；Enter/失焦立即提交；提交写回 sliderQ、置 dirty 并刷新。自动推荐模式下用户手改 Q 自动切回手动态。
- **单向泄漏杜绝**：`setKeyboardTracking(False)`，上下箭头/滚轮仍即时更新。

**回归**：
- `test_q_spinbox_writes_back_to_slider`（3.21 → slider ≈3.21 公差内）。
- `test_q_spinbox_debounce_commit_without_focus`（输入后 debounce 计时启动并能自动提交，无需失焦）。

---

## 5. BUG6：删除"参考曲线：Q=..."说明行

`hintLabel` 置 `None` 彻底移除；相关字段不再生成说明文本（在 `test_no_old_symbols_in_ui` 等既有测试中覆盖）。

---

## 6. BUG4：Hover 数据框高对比设计

`plot_widget.py` 新增 `_HOVER_*` 高对比配色（深色底 + 高亮文字，与 Q 族配色体系完全解耦），自适应位置（越界翻转避免被窗口边缘裁切）；tooltip 行统一为 `(style, text)` 元组，`test_fs_display_format_in_tooltip` 已适配该格式。

---

## 7. BUG7：整体 UI 统一视觉升级（性能无损）

`main.py` 顶部 `APP_QSS` 统一视觉语言：
- 浅灰底 `#F5F7FA`，白色圆角卡片（`QGroupBox`/`resultCard` 8px/6px 圆角 + 细腻描边）。
- 统一字号/配色（标题 14pt 加粗、正文 9.5pt、主文字 `#1F2937`、次级 `#334155/#64748B`）；输入框聚焦高亮 `#2563EB`。
- **不改动绘图层**：视觉全部落到 widget 层样式，QPainter 曲线绘制路径不重入，绘制性能不受影响（`bench_k_drag` 中位数维持 ~14ms 级别，见 §9）。

---

## 8. BUG9/BUG10：右侧结果区改卡片 + 关键结果加 Re + 中文变量名

`src/main.py` 新增 `ResultCardsView(QScrollArea)`：**一次性创建** 6 张卡片（当前工作点 / 谐振腔参数 / 调频范围 / 最坏工况·FHA / 设计分析 / 建议），仅更新 `QLabel` 文本，不重建 widget → 滚动位置天然稳定、性能优；兼容旧契约 `toPlainText()/verticalScrollBar()`。

- **BUG10 中文命名**：关键结果显示 `所需最小增益 / 所需最大增益 / 满载 Q / 过载 Q / 最低归一化频率 / 最高归一化频率` 等中文变量。
- **关键结果新增 Re**：等效 AC 负载 `Re` 及其单位 Ω 进入默认关键区（源自 `llc_report.py`，`Re=8·n²·Vo²/(π²·Pout)`，TI 语义）。

**回归/既有断言**：`test_engineering_gui`、`test_gui_smoke` 更新为中文关键字段 + Re/Ω/Lr/Lm/Cr 检查；新增 `test_result_cards_layout_with_chinese_fields`。

---

## 9. BUG3/BUG12：标签输入框间距、状态提示、显示选项美化（含性能）

- BUG3：参数调节区标签-输入框 proximity 重排（`params_grid` 同宽控件、紧凑行距）。
- BUG12a 状态提示：`engStatusLabel` 显示"已自动应用 / 参数有未完成输入"状态反馈（拖动态节流）。
- BUG12b 显示选项：`显示选项` 复选框组统一样式（勾选间距、文字配色），仅影响显隐，绝不触发无关数学重算。
- 本品类均以"布局/样式"为主，不触碰 LLC 数学路径。

---

## 10. GUI 验收（真实渲染）

**真实 EXE 启动**（`scripts/accept_exe.ps1` + `scripts/verify_exe.py`）：
```
PASS: visible window found. title='LLC 谐振变换器交互式多增益曲线'
PASS: IsWindowVisible = True
PASS: clean close exited=True
OK: main window found; OK: survived resize/repaint
```

**默认界面截图**（`scripts\_sc2_default_ui.png`，真实单文件 EXE 1434×998）：顶部"西安电子科技大学 张名扬"、卡片结果区（图例/当前 Q 粗线/工作点/fnp·fnr=1·阻容分界线三特征线）、底部"工程参数设置"面板、"当前 Q"数字输入框（300ms 自动生效说明）全部正常渲染。

**真实窗口拖动验收**（`scripts/accept_ui_v2.py`，真实 QPainter + `widget.grab()`）：
| 场景 | 内容 | 结果 |
|---|---|---|
| S1 | 默认界面含签名/卡片/工程参数设置/ResultCardsView | PASS |
| S2 | 驱动 K 0→1000 全程（preview on→off），正式 `family_y` 全有限、无 NaN、参考族清晰细线（无彩色色带），松手恢复全精度 | PASS |
| S3 | Q 数字框 `2.0` 经 debounce 自动提交，slider≈1.9975 | PASS |

截图：`scripts\_sc2_S1_default.png`、`_sc2_S2_drag_in_preview.png`、`_sc2_S3_drag_released.png`、`_sc2_S4_Qspin_commit.png`。

---

## 11. 回归测试清单（新增 9 项，`tests/test_perf_fix.py`）

`test_preview_uses_separate_data_array` / `test_preview_reduced_sampling_length` / `test_exit_preview_recomputes_full_family` / `test_preview_hidden_reference_still_pollution_free` / `test_top_banner_shows_school_and_name` / `test_engineering_panel_renamed` / `test_q_spinbox_writes_back_to_slider` / `test_q_spinbox_debounce_commit_without_focus` / `test_result_cards_layout_with_chinese_fields`。

**pytest：1150 passed, 0 failed**（含既有 1141 项，数学功能零回退）。

---

## 12. 修改文件 / 新增脚本

**修改**：`src/main.py`、`src/plot_widget.py`、`src/llc_report.py`、`tests/test_perf_fix.py`、`tests/test_engineering_gui.py`、`tests/test_gui_smoke.py`、`tests/test_ui_interaction.py`

**新增**：`scripts/accept_ui_v2.py`

---

## 13. 交付产物

- 新 EXE：`F:\OneDrive\找工作\电源\LLC\LLC增益曲线_EXE\dist\LLC增益曲线.exe`
- 大小：`21,859,609 bytes`（20.85 MB）< 24 MB ✅
- SHA256：`CB939DA42F1465FA09233588F9AF07B81CFA4FF06EC1938FE57DC40E211C25C6`
- 本地产物均未 push GitHub。